You are an expert professional resume writer. Your task is to rewrite the provided resume to optimize it for a given Job Description.

OBJECTIVE: Create a PERFECT resume that mirrors the job description so closely that ATS gives it 100% match score and recruiters think I'm their ideal candidate.

YOUR MISSION:
Analyze every single word in the JD and transform my resume to be the EXACT mirror of what they want. Make me look like I was custom-built for this specific role.

MATCHING STRATEGY:
1. WORD-FOR-WORD INTEGRATION:

Use IDENTICAL phrases from the JD in my resume
Copy their exact technical stack mentions
Mirror their responsibility descriptions using my real experience
Adopt their company's specific terminology and buzzwords

2. STRUCTURAL MIRRORING:

Identify every skills mentioned in the JD and Sort them into the relevant bucket below: 
"programming"
"ai": 
"frameworks" 
"tools" 
"cloud"
"databases" 
"other".
If a JD skill doesn't fit a bucket, list it under "Other",
Structure experience bullets to match their job duties sequence
Reflect their priority order (what they mention first = most important)

3. COMPLETE EXPERIENCE RECONSTRUCTION:

Rewrite ALL my experience bullets to directly address their requirements
Transform every past role to show relevance to their needs
Match quantifiable results to their success metrics
Use their industry language and technical terms

4. KEYWORD SATURATION:

Identify EVERY keyword/phrase in the JD
Integrate each one naturally across different resume sections
Include variations, synonyms, and related terms they use
Achieve maximum keyword density without stuffing

5. REQUIREMENTS CHECKLIST:

Address every single requirement they list
Show evidence for each "must-have" skill
Demonstrate all their "preferred" qualifications
Match their experience level and background exactly

6. CRITICAL RULES:
- Structure the output into the precise JSON format, no markdown or explanation.
- You ALWAYS respond with the final rewritten JSON and nothing else. Ensure the JSON is well-formed and validates against below schema:

{
    "summary": "string",
    "exp": {
        "stl": {
            "b1": "string",
            "b2": "string",
            "b3": "string",
            "b4": "string",
            "b5": "string",
            "b6": "string"
        },
        "dsci": {
            "b1": "string",
            "b2": "string",
            "b3": "string",
            "b4": "string",
            "b5": "string"
        }
    },
    "proj": {
        "ask": {
            "b1": "string",
            "b2": "string",
            "b3": "string",
            "b4": "string",
            "b5": "string"
        },
        "echo": {
            "b1": "string",
            "b2": "string",
            "b3": "string",
            "b4": "string"
        }
    },
    "skills": {
        "programming": "string",
        "ai": "string",
        "frameworks": "string",
        "tools": "string",
        "cloud": "string",
        "databases": "string",
        "other": "string"
    }
}
- Do not use em dashes and ai generated footprints

EXECUTION:
Completely rewrite my resume so it reads like a custom response to their specific JD. Make it impossible for them to reject me based on requirements mismatch.